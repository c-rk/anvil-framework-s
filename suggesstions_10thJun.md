Make a copy of this version of canvas as v1 and don't touch that (just in case we wanna get back to this), then work on these things:

* I like the canvas, good wiring. Add arithmetic blocks (like add, multiply, log, trig etc..) since it is also a calculator too to be applied to outputs or to quantities.
* It would be good to have input Q blocks, that take in quantity from a relation and then maybe we could apply some arithmetic to it before feeding it to another system or relation
* Also use anvil.viz module to add plot blocks to be able to add plots to the canvas quickly or maybe integrate the sweep also inside the builder canvas.
* Change all RSQ names to courier and all the other headings, description and instructive text to times new roman serif type.
* It would be nice to have a save function, like after I make a canvas, I'd like to save it to reload it whenever I need.
* Make an example canvas for all the examples available.
* Also, add canvas blocks or relations to use with csv files to take in like time signals to signal processing, or take in other data and then wire each column as a quantity to sweep through the rows for a relation or a system and then plot it
* Also add save or export RSQs in the canvas to save the data.
* Just a simple calculator page and then a canvas page. A global spotlight kinda search would be nice to have to quickly search for RSQs.
* Any new RSQs added to the database should immediately be accessible to the web interface.
* All RSQs on the canvas must be grouped by domain.
* Some of the sweeps of relations need input of other quantites that are not swept, and there is no place to input that quantity and that's why I think including sweep, watchdog, viz and everthing else  inside the canvas is helpful.
* Simple calc can be just normal calc for a relation. Also implement like a memory function like in a calculator to store certain result number or quantity with its units that can be input elsewhere in the calculator.
* In calculator, for signal processing relations, they need a time series value and not just a single value, so there must some way to feed that or reference that.
* In calculator no way to specify units of input qty. Include ways to do that too.
* Also, I don't think adapters or relation adapters can be used in the calculator. Just have things that can be done in a calculator.
* The canvas is the all spanning stuff.
* For the adapters made, don't want a mock mode. Remove that, it works only with the module installed, if not and someone tries to call it, it just gives an error saying module not installed or found.
* Also make a favicon of an aesthetic anvil b/w for the we version, should not look like a commie sign
* Use a b/w color pallete across the web, no need for orange or anything else. Include keyboard shortcuts and a quick lookup for shortcuts and link to access the documentation for everthing that opens up the corresponding documentation page on a new tab.
* If terminal server is ended, it must show on the web that connection has ended or something like that.

